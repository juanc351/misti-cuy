export const LOADER = {
  /**
   * Tiempo mínimo que el loader permanece visible.
   */
  minimumDuration: 300,

  /**
   * Duración de una vuelta completa del segmento.
   */
  rotationDuration: 900,

  /**
   * Duración del fade de entrada.
   */
  fadeInDuration: 180,

  /**
   * Duración del fade de salida.
   */
  fadeOutDuration: 180,
} as const;