/**
 * ============================================================================
 * MISTI CUY
 * ----------------------------------------------------------------------------
 * Navigation System
 *
 * Tipos oficiales del sistema de navegación.
 * ============================================================================
 */

/**
 * Item del menú principal.
 */
export interface NavigationItem {
  id: string;
  label: string;
  title: string;
  href: string;
  isEnabled: boolean;
}

/**
 * Item del menú principal.
 */
export interface NavigationItem {
  id: string;
  label: string;
  title: string;
  href: string;
  isEnabled: boolean;
}
